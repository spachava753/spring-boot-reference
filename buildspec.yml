version: 0.2

phases:
  install:
    runtime-versions:
      java: openjdk8
    commands:
      - apt-get install maven -y
  build:
    commands:
      - mvn install -DskipTests=true
artifacts:
  files:
    - target/refapp.war
    - appspec.yml
